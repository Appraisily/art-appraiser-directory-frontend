# No required build-time env vars for this frontend at the repo level.
# `PORT` is provided by Docker at runtime (8080).
# GTM resolves from runtime `GOOGLE_TAG_MANAGER_ID` on appraisily.com/__ENV__.js,
# or falls back to the shared default container when absent.
